{
  "name": "@homesvr/config",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsup src/env.ts --format esm,cjs --dts --out-dir dist",
    "lint": "eslint \"src/**/*.ts\"",
    "dev": "tsup src/env.ts --watch --format esm,cjs --dts --out-dir dist"
  },
  "dependencies": {
    "zod": "3.23.8"
  },
  "devDependencies": {
    "tsup": "8.0.2"
  }
}
